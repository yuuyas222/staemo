<%= render "forms/header"%>
<div>
 <p>新規投稿画面</p>
 <%= attachment_image_tag current_user, :profile_image, format: 'jpeg', size: "100x100" %>
 <%= current_user.name %>
 <%= form_for(@emotion) do |f| %>
 <%= f.text_area :body %>
 <%= f.submit "投稿する"%>   
 <% end %>
</div>